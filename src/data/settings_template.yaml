# LLM to be used for section generation based on transcript and presentation
# Available providers and models: https://models.litellm.ai/
llm_model: gemini/gemini-2.0-flash

# API key for the LLM provider.
llm_api_key:

# System index of the microphone to be used by default during the presentation control
# To see available microphones, run `moves settings list`
selected_mic: 0
